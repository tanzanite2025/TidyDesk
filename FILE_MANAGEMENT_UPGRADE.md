# 文件管理升级 - 移动到隐藏存储

## 升级日期
2026-05-24

## 问题背景

### 用户反馈
用户提出了一个核心的用户体验问题：

> "拖入文件后，原文件还在桌面，那拖进来的意义在哪里？为了不失效，不是应该拖进来就隐藏桌面的这个，才能保证既不影响文件路径、不担心更新，又能很好整理桌面。"

### 原有方案的问题
**旧方案**: 创建快捷方式，原文件保留在桌面
- ❌ 桌面文件依然存在，没有达到整理效果
- ❌ 用户困惑：拖进来了为什么桌面还有？
- ❌ 无法真正清理桌面

## 新方案设计

### 核心思路
**移动到隐藏存储 + 快捷方式访问**

1. **拖入时**: 将文件从桌面移动到 TidyDesk 的隐藏存储目录
2. **桌面**: 原文件消失，桌面整洁
3. **抽屉**: 创建指向存储位置的快捷方式，可以正常访问
4. **还原**: 提供"还原到桌面"功能，可以将文件移回桌面

### 技术架构

```
用户操作流程:
┌─────────────┐
│ 拖动文件到抽屉 │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────┐
│ 1. 移动文件到隐藏存储            │
│    C:\Users\...\AppData\        │
│    Roaming\TidyDesk\storage\    │
│    {timestamp}-{random}/        │
│    原文件名                      │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ 2. 在抽屉中创建快捷方式          │
│    指向存储位置的 .lnk 文件      │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ 3. 桌面文件消失                  │
│    ✅ 桌面整洁                   │
└─────────────────────────────────┘

还原流程:
┌─────────────┐
│ 点击还原按钮 │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────┐
│ 1. 将文件从存储移回桌面          │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ 2. 删除抽屉中的快捷方式          │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ 3. 清理空的存储目录              │
└─────────────────────────────────┘
```

## 实现细节

### 1. 存储目录结构

```
C:\Users\{用户}\AppData\Roaming\TidyDesk\
├── drawers\              # 抽屉目录（快捷方式）
│   ├── 收纳抽屉\
│   │   ├── 文档.docx.lnk
│   │   └── 图片.png.lnk
│   └── 工作文件\
│       └── 报告.pdf.lnk
└── storage\              # 文件存储（实际文件）
    ├── 1716537600000-abc123\
    │   └── 文档.docx
    ├── 1716537601000-def456\
    │   └── 图片.png
    └── 1716537602000-ghi789\
        └── 报告.pdf
```

### 2. 后端实现

#### 新增函数: `getFileStorageRoot()`
```javascript
function getFileStorageRoot() {
  return path.join(app.getPath('userData'), 'storage');
}
```

#### 重写函数: `moveFileToDrawer()`
```javascript
async function moveFileToDrawer(sourcePath, targetDir) {
  // 1. 验证源文件
  // 2. 检查是否来自桌面
  // 3. 创建唯一存储目录
  // 4. 移动文件到存储（桌面文件）或复制（非桌面文件）
  // 5. 创建指向存储位置的快捷方式
  // 6. 返回结果
}
```

**关键逻辑**:
- 来自桌面的文件 → **移动**到存储（桌面文件消失）
- 来自其他位置的文件 → **复制**到存储（保留原文件）
- 快捷方式（.lnk/.url）→ 直接复制到抽屉

#### 新增 IPC: `restore-to-desktop`
```javascript
ipcMain.handle('restore-to-desktop', async (_event, { shortcutPath }) => {
  // 1. 验证快捷方式在抽屉中
  // 2. 解析快捷方式目标
  // 3. 检查目标是否在 TidyDesk 存储中
  // 4. 移动文件回桌面
  // 5. 删除快捷方式
  // 6. 清理空的存储目录
});
```

### 3. 前端实现

#### 新增 UI: 还原按钮
```tsx
{!isInvalid && onRestore && (
  <button
    onClick={onRestore}
    className="rounded-md p-1 text-slate-500 hover:bg-emerald-500/15 hover:text-emerald-200"
    title="还原到桌面"
  >
    <Undo2 size={13} />
  </button>
)}
```

**显示条件**:
- 文件有效（`isValid !== false`）
- 文件在 TidyDesk 存储中（`targetPath` 包含 `storage`）

#### 新增函数: `handleRestoreToDesktop()`
```typescript
async function handleRestoreToDesktop(fileId: string, fileName: string) {
  // 1. 确认对话框
  // 2. 调用 restoreToDesktop API
  // 3. 刷新文件列表
  // 4. 显示成功/失败通知
}
```

## 用户体验改进

### 拖入文件
**之前**:
1. 拖动文件到抽屉
2. 桌面文件还在 ❌
3. 抽屉中有快捷方式
4. 用户困惑：为什么桌面还有？

**现在**:
1. 拖动文件到抽屉
2. 桌面文件消失 ✅
3. 抽屉中可以访问文件
4. 桌面整洁，目的达成 ✅

### 还原文件
**新功能**:
1. 鼠标悬停在文件上
2. 点击"还原"按钮（↩️ 图标）
3. 确认对话框
4. 文件移回桌面，抽屉中的快捷方式删除

## 安全性保障

### 1. 文件来源检测
```javascript
const isFromDesktop = isPathInside(sourcePath, desktopPath);
```
- 只有来自桌面的文件才会被移动
- 其他位置的文件会被复制（保留原文件）

### 2. 系统文件保护
```javascript
const systemPaths = [
  'C:\\Windows',
  'C:\\Program Files',
  'C:\\Program Files (x86)',
  process.env.SYSTEMROOT,
  process.env.WINDIR
];
```
- 禁止移动系统目录中的文件

### 3. 存储隔离
- 每个文件存储在独立目录中
- 使用时间戳 + 随机字符串命名
- 避免文件名冲突

### 4. 还原验证
```javascript
const isInStorage = isPathInside(targetPath, storageRoot);
if (!isInStorage) {
  throw new Error('File is not managed by TidyDesk');
}
```
- 只能还原 TidyDesk 管理的文件
- 防止误操作

## 兼容性处理

### 快捷方式文件
- `.lnk` 和 `.url` 文件直接复制到抽屉
- 不移动到存储（保持原有行为）
- 如果来自桌面，删除桌面上的快捷方式

### 非桌面文件
- 从其他位置拖入的文件会被复制
- 原文件保留在原位置
- 避免破坏用户的文件组织

### 旧版本数据
- 旧版本创建的快捷方式（指向桌面文件）继续有效
- 不会自动迁移到新存储
- 用户可以手动还原后重新拖入

## 测试场景

### 基本功能测试
1. ✅ 拖动桌面文件到抽屉 → 桌面文件消失
2. ✅ 双击抽屉中的文件 → 正常打开
3. ✅ 点击还原按钮 → 文件回到桌面
4. ✅ 拖动非桌面文件 → 原文件保留

### 边界情况测试
1. ✅ 拖动快捷方式 → 直接复制，不进入存储
2. ✅ 拖动系统文件 → 拒绝操作
3. ✅ 还原非 TidyDesk 管理的文件 → 拒绝操作
4. ✅ 存储目录被手动删除 → 快捷方式显示失效

### 文件完整性测试
1. ✅ 移动大文件（>100MB）→ 完整性校验
2. ✅ 移动文件夹 → 递归移动所有内容
3. ✅ 文件名包含特殊字符 → 正确处理
4. ✅ 同名文件冲突 → 自动重命名

## 修改的文件

### 后端
- `electron/main.cjs`
  - 新增 `getFileStorageRoot()` 函数
  - 重写 `moveFileToDrawer()` 函数
  - 保留 `createDrawerShortcut()` 作为别名
  - 新增 `restore-to-desktop` IPC 处理器
  - 更新 `prepareStorage()` 初始化存储目录

### 前端
- `electron/preload.cjs`
  - 新增 `restoreToDesktop` API 暴露
  
- `src/types/file.ts`
  - 已有 `icon` 字段（用于图标显示）
  
- `src/context/WorkspaceContext.tsx`
  - 新增 `restoreToDesktop` 函数
  - 更新 `WorkspaceContextType` 接口
  
- `src/App.tsx`
  - 导入 `Undo2` 图标
  - 更新 `FileTile` 组件添加 `onRestore` 参数
  - 新增 `handleRestoreToDesktop` 函数
  - 在 FileTile 调用中添加还原按钮

## 性能影响

### 存储空间
- 文件存储在 `AppData\Roaming\TidyDesk\storage`
- 每个文件占用原始大小的空间
- 建议定期清理不需要的文件

### 操作性能
- 移动文件比复制快（同一磁盘）
- 大文件移动可能需要几秒钟
- 异步操作，不阻塞 UI

### 内存使用
- 文件本身不加载到内存
- 只存储文件路径和元数据
- 内存占用可忽略不计

## 用户指南

### 如何使用
1. **整理桌面**: 将桌面文件拖入抽屉，桌面立即整洁
2. **访问文件**: 双击抽屉中的文件，正常打开
3. **还原文件**: 鼠标悬停 → 点击还原按钮 → 确认

### 注意事项
- ⚠️ 从桌面拖入的文件会被移动（桌面文件消失）
- ⚠️ 从其他位置拖入的文件会被复制（原文件保留）
- ⚠️ 还原操作会将文件移回桌面，抽屉中的快捷方式删除
- ⚠️ 删除抽屉中的快捷方式不会删除存储中的文件（需要手动清理）

### 常见问题

**Q: 拖入的文件存储在哪里？**
A: `C:\Users\{用户}\AppData\Roaming\TidyDesk\storage\`

**Q: 如何清理存储空间？**
A: 目前需要手动删除 storage 目录中的文件。未来版本会添加自动清理功能。

**Q: 删除快捷方式会删除原文件吗？**
A: 不会。删除快捷方式只删除抽屉中的入口，存储中的文件保留。

**Q: 可以还原旧版本创建的快捷方式吗？**
A: 不能。只有新版本移动到存储的文件才能还原。旧版本的快捷方式指向桌面文件。

## 下一步计划

### v3.1.0 计划功能
1. **存储管理界面**
   - 查看存储空间使用情况
   - 清理孤立文件（没有快捷方式的文件）
   - 批量还原文件

2. **智能清理**
   - 自动检测孤立文件
   - 定期清理提醒
   - 存储空间警告

3. **文件同步**
   - 支持多设备同步（OneDrive/Dropbox）
   - 云端备份
   - 版本历史

## 发布信息

- **版本号**: v3.1.0
- **发布日期**: 待定
- **重要性**: 重大功能更新
- **向后兼容**: 是（旧版本快捷方式继续有效）

## 相关文档
- `TASKBAR_AND_FILENAME_FIXES.md` - 任务栏和文件名修复
- `ICON_DISPLAY_FIX.md` - 文件图标显示修复
- `v3.0.2_FIXES_SUMMARY.md` - v3.0.2 修复总结
